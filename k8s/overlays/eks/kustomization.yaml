apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
images:
  - name: sarif
    newName: 702516017596.dkr.ecr.us-west-2.amazonaws.com/sarif
    newTag: "5012516"
patches:
  - path: ingress-patch.yaml
# CORS_ORIGIN is left at the base default (frontend + API are same-origin behind
# one ALB). Add configmap-cors-patch.yaml only if a real browser CORS failure
# shows up during verification.
